# دليل تشغيل ونشر موقع "منارة العلم" — خطوة بخطوة


## المرحلة 1: تجهيز قاعدة البيانات (Supabase) — 10 دقائق

1. اذهب إلى https://supabase.com وأنشئ حسابًا مجانيًا.
2. اضغط **New Project**. اختر اسمًا وكلمة مرور لقاعدة البيانات (احفظها).
3. بعد إنشاء المشروع، اذهب إلى **SQL Editor** من القائمة الجانبية.
4. افتح ملف `supabase/schema.sql` من هذا المشروع، انسخ محتواه كاملاً، الصقه في SQL Editor، واضغط **Run**.
   - هذا سينشئ كل الجداول (المستخدمين، الكتب، المشايخ، التقييمات، إلخ) تلقائيًا.
5. اذهب إلى **Project Settings > API** وانسخ:
   - `Project URL` → هذا هو `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public key` → هذا هو `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `service_role key` (اضغط Reveal) → هذا هو `SUPABASE_SERVICE_ROLE_KEY` (سري جدًا!)
6. اذهب إلى **Storage** من القائمة الجانبية → أنشئ Bucket جديد اسمه `books-pdf` واجعله Public.
   - من هنا سترفع ملفات PDF للكتب لاحقًا، وتحصل على رابط لكل ملف تضعه في جدول books.

---



## المرحلة 4: النشر ليصبح متاحًا لكل الناس (Vercel) — 10 دقائق

**Vercel** هي الشركة التي تستضيف مواقع Next.js مجانًا، وتعطيك رابطًا يعمل فورًا على كل الأجهزة.

1. ارفع مجلد المشروع إلى GitHub:
   - أنشئ حسابًا على https://github.com إن لم يكن لديك.
   - أنشئ Repository جديد (خاص أو عام)، وارفع له كل ملفات هذا المشروع.
2. اذهب إلى https://vercel.com وسجل دخول بحساب GitHub.
3. اضغط **Add New Project** واختر الـ Repository الذي رفعته.
4. قبل الضغط على Deploy، افتح قسم **Environment Variables** وأضف نفس المتغيرات الأربعة:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `OPENAI_API_KEY`
5. اضغط **Deploy**. بعد دقيقة أو دقيقتين، ستحصل على رابط مثل:
   `https://islamic-library-yourname.vercel.app`
   هذا الرابط يعمل فورًا على كل الجوالات (آيفون، أندرويد) وكل المتصفحات — بدون تحميل أي تطبيق.

6. (اختياري) اربط دومين خاص بك (مثل منارة-العلم.com) من إعدادات المشروع في Vercel > Domains.

---

## المرحلة 5: إضافة المحتوى الفعلي

الموقع فارغ حاليًا إلا من 3 مشايخ تجريبيين و5 أصناف. لإضافة كتب حقيقية:

1. من لوحة Supabase اذهب إلى **Table Editor**.
2. جدول `books`: أضف صفًا لكل كتاب (اربطه بـ scholar_id و category_id، وارفع ملف PDF في Storage
   ثم انسخ رابطه في خانة pdf_url).
3. جدول `quiz_questions`: أضف أسئلة الاختبار لكل كتاب.
4. جدول `knowledge_chunks`: هذا الأهم للمساعد الذكي — أضف فيه مقاطع/أحاديث حقيقية من الكتب
   مع ذكر المرجع الدقيق (مثال: "رياض الصالحين، حديث رقم 1")، حتى يستطيع الشات بوت
   الاستشهاد بها بدقة. كلما أضفت مقاطع أكثر، كانت إجابات المساعد أدق وأشمل.

---

## ملاحظات مهمة قبل الإطلاق للعموم

- **المحتوى الديني**: يُنصح بشدة أن يراجع أهل علم موثوقون كل نص يُضاف لجدول `knowledge_chunks`
  وكل ملف PDF قبل نشره، لضمان دقة المصادر ونسبتها الصحيحة.
- **الأمان**: لا تشارك `SUPABASE_SERVICE_ROLE_KEY` أو `OPENAI_API_KEY` مع أحد، ولا ترفعهما على GitHub
  (ملف `.env.local` مستثنى تلقائيًا عبر `.gitignore`).
- **التكلفة**: Supabase وVercel مجانيان لحجم استخدام متوسط. OpenAI يُحاسَب حسب الاستخدام
  (عادة بضعة دولارات شهريًا لموقع متوسط الزيارات).
- هذا الكود **نسخة أولى functional** تغطي كل الميزات المطلوبة — يُنصح بمراجعة أمنية واختبار
  شامل قبل إطلاقه بشكل عام على نطاق واسع.
